export const accordion = (trigerSelector: string) => {
    const btns: NodeListOf<HTMLElement> = document.querySelectorAll(trigerSelector); 

    btns.forEach(btn => {
        btn.addEventListener('click', function() {
            this.
        })
    })
}